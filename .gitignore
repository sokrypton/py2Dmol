.DS_Store

# Backup files
*.bak
*.backup
*.tmp
*~

# Plan/documentation files
implementation_plan.md

# IDE
.vscode/
.idea/
*.cif
.ipynb_checkpoints/
*.json
node_modules/
*.egg-info/

# cartoon test harness output (regenerate with python tests/build.py)
tests/out/
__pycache__/

# Benchmark input for `python tests/ss_bench.py --build` (DSSP ground truth).
# 3.8 MB and not needed to run the viewer; place it in the repo root to rebuild.
natives.zip

# setuptools build directory
build/
dist/
*.pdb
*.a3m
*.zip

# The browser probes each write a page next to index.html and delete it after -
# _pypage.html, _ligweb.html and so on. One survived a crash and was committed
# (_panelpic2.html, 1,022 lines of nothing).
_*.html
